{
  "$schema": "https://json-schema.org/schema",
  "$id": "GoLib",
  "title": "Go Library Generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the Go library",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the library name would you like to use?"
    },
    "modulePrefix": {
      "type": "string",
      "description": "The module prefix for the Go library",
      "default": "github.com/org/project",
      "x-prompt": "What is the module prefix for the Go library?"
    }
  },
  "required": ["name"]
}
